# 4399 注册接口客户端依赖
# 安装： pip install -r requirements.txt
#
# 说明：
#   requests      负责会话（USESSIONID cookie 保持）与表单提交
#   pycryptodome  提供 AES-256-CBC + 手动 PKCS7 与 EVP_BytesToKey(MD5)
#
# 注意：这里用的是 pycryptodome（导入名 Crypto），不是已被弃用的 pycrypto。
#       两者导入名相同，若环境里装过 pycrypto 请先卸载，否则会出现
#       "ModuleNotFoundError: No module named 'Crypto.Cipher._mode_cbc'" 之类的怪问题。

requests>=2.28,<3.0
pycryptodome>=3.18,<4.0
